trait Node
trait Leaf extends Node {
    Node data
}
trait Tree extends Node {
    Node left
    Node right
    Node parent
}

object BinaryTree as Tree {
    override Node left
    override Node right
    override Node parent
}

def main() {
    var tree = BinaryTree()
}
